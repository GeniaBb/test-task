import { CraneTypePipe } from './crane-type.pipe';

describe('CraneTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CraneTypePipe();
    expect(pipe).toBeTruthy();
  });
});
